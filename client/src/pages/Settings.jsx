// client/src/pages/Settings.jsx
import React from 'react';
import { Typography } from '@mui/material';

function Settings() {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        设置
      </Typography>
      <Typography variant="body1">这里是设置页面。</Typography>
      {/* 添加更多设置选项 */}
    </div>
  );
}

export default Settings;
